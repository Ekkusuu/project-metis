fastapi[standard]>=0.121.0
uvicorn>=0.38.0
pyyaml>=6.0
chromadb>=0.4.22
sentence-transformers>=2.2.2
transformers>=4.30.0
requests>=2.31.0

# NOTE: PyTorch is required but is installed separately by the setup scripts.
# Local macOS installs use the default PyTorch wheels, which enable Apple Silicon MPS when available.
# NVIDIA setups can still point at a CUDA wheel index via `TORCH_INDEX_URL`.
# Do NOT add a plain 'torch' line here because the exact wheel can vary by platform/runtime.
